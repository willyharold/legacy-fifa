{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="police-text" style="text-align: center;font-size: 40px;">Modification d'actualitée</h1>
    <div style="margin-left: 10%;margin-right: 10%;">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div style="text-align: center;">
            <a class="btn btn-info" href="{{ path('actualite_index') }}">Retour</a>
            <input type="submit" value="Modifier" class="btn btn-success"/>
        </div>
    {{ form_end(edit_form) }}

    </div>
{% endblock %}
