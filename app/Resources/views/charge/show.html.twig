{% extends 'base.html.twig' %}

{% block body %}
    <h1>Charge</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ charge.id }}</td>
            </tr>
            <tr>
                <th>Recompense1</th>
                <td>{{ charge.recompense1 }}</td>
            </tr>
            <tr>
                <th>Recompense2</th>
                <td>{{ charge.recompense2 }}</td>
            </tr>
            <tr>
                <th>Recompense3</th>
                <td>{{ charge.recompense3 }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('charge_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('charge_edit', { 'id': charge.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
