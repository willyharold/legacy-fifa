{% extends 'base.html.twig' %}

{% block body %}
    <h1>Point</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ point.id }}</td>
            </tr>
            <tr>
                <th>Nbrepoints</th>
                <td>{{ point.nbrePoints }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('point_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('point_edit', { 'id': point.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
